# Core RAG stack
langchain>=0.3.0,<0.4.0
langchain-openai>=0.2.0,<0.3.0
langchain-community>=0.3.0,<0.4.0
langchain-chroma>=0.1.4,<0.2.0
langchain-text-splitters>=0.3.0,<0.4.0

# Raw OpenAI SDK (already an indirect dep of langchain-openai, pinned
# explicitly since retrieval/qa.py now calls the Moderation endpoint
# directly -- input-abuse guardrail, not the chat/generation path).
openai>=1.40.0,<2.0.0

# Vector store
chromadb>=0.5.0,<0.6.0
# Pinned to avoid a known chromadb 0.5.x / posthog telemetry signature
# mismatch that spams harmless "capture() takes 1 positional argument..."
# warnings on every Chroma client init.
posthog<3.0.0

# API serving layer (api.py)
fastapi>=0.115.0,<0.120.0
uvicorn[standard]>=0.30.0,<0.35.0
python-multipart>=0.0.9,<0.1.0

# Persistence for user accounts, chat history + document library
# (assistant/db.py). Separate from the Temporal Postgres instance in
# docker-compose.yml (that one is Temporal's internal store, not
# application data).
psycopg[binary]>=3.1,<4.0
psycopg-pool>=3.2,<4.0

# Auth (assistant/auth.py): bcrypt for password hashing, PyJWT for issuing
# and verifying the JWTs /ask, /ingest, etc. all require.
bcrypt>=4.0.0,<5.0.0
pyjwt>=2.8.0,<3.0.0

# Live-config cache (assistant/config_store.py) -- read path for hot
# app config (system prompts, retrieval tuning) backed by Postgres.
redis>=5.0,<6.0

# Ingestion orchestration (assistant/orchestration/, worker.py) -- durable,
# per-file-retryable ingestion workflow/activities
temporalio>=1.7.0,<2.0.0

# PDF parsing / chunking
pymupdf4llm>=0.0.17,<0.1.0   # PDF -> Markdown extraction (pulls in PyMuPDF/fitz automatically)
tiktoken>=0.7.0,<1.0.0       # cl100k_base token counting for chunk sizing

# OCR fallback for scanned pages
pytesseract>=0.3.10,<0.4.0
pillow>=10.0.0,<12.0.0

# Config
python-dotenv>=1.0.1,<2.0.0

# Eval harness (eval/run_eval.py) -- parses golden_questions.yaml
pyyaml>=6.0,<7.0
