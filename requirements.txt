# Core RAG stack
langchain>=0.3.0,<0.4.0
langchain-openai>=0.2.0,<0.3.0
langchain-community>=0.3.0,<0.4.0
langchain-chroma>=0.1.4,<0.2.0
langchain-text-splitters>=0.3.0,<0.4.0

# Vector store
chromadb>=0.5.0,<0.6.0
# Pinned to avoid a known chromadb 0.5.x / posthog telemetry signature
# mismatch that spams harmless "capture() takes 1 positional argument..."
# warnings on every Chroma client init.
posthog<3.0.0

# UI
streamlit>=1.38.0,<2.0.0

# API serving layer (api.py) + Streamlit's HTTP client to it (app.py)
fastapi>=0.115.0,<0.120.0
uvicorn[standard]>=0.30.0,<0.35.0
python-multipart>=0.0.9,<0.1.0
requests>=2.31.0,<3.0.0

# PDF parsing / chunking
pymupdf4llm>=0.0.17,<0.1.0   # PDF -> Markdown extraction (pulls in PyMuPDF/fitz automatically)
tiktoken>=0.7.0,<1.0.0       # cl100k_base token counting for chunk sizing

# OCR fallback for scanned pages
pytesseract>=0.3.10,<0.4.0
pillow>=10.0.0,<12.0.0

# Config
python-dotenv>=1.0.1,<2.0.0

# Eval harness (eval/run_eval.py) -- parses golden_questions.yaml
pyyaml>=6.0,<7.0
